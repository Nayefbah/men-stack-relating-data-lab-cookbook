<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/Style/style.css" />
    <title><%= ingredient.name %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <main class="recipe-container">
      <h1 class="recipe-name"><%= ingredient.name %></h1>
      <h2 class="recipe-owner">
        <strong>Owner:</strong> <%= ingredient.owner.username %>
      </h2>
      <section class="recipe-actions">
        <a href="/ingredients/<%= ingredient._id %>/edit" class="btn btn-edit"
          >Edit Ingredients</a
        >
        <div>
          <form
            action="/ingredients/<%= ingredient._id %>?_method=DELETE"
            method="POST"
            class="delete-form"
          >
            <button type="submit" class="btn btn-delete">
              Delete Ingredient
            </button>
          </form>
        </div>
      </section>
    </main>
  </body>
</html>
